<resources>

    <!-- Base application theme. -->
    <style name="SpalshScreenTheme" parent="Theme.AppCompat.NoActionBar">

    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="App.Style.Spinner" parent="@android:style/Theme.Holo.Light">
        <item name="android:divider">#333333</item>
        <item name="android:dividerHeight">100dp</item>
        <item name="android:textSize">50sp</item>
    </style>
</resources>
